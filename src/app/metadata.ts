import { generateMetadata } from '@/lib/metadata';
import type { Metadata } from 'next';

export const metadata: Metadata = generateMetadata(
  'D4L - Decentralized for Life | Next-Gen Token Platform',
  'D4L is a revolutionary decentralized platform for token management, airdrops, and blockchain interactions. Join our community today!',
  '/',
  '/images/home-og.jpg'
);
